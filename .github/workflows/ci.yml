on:
  pull_request:
  push:
    branches:
      - Main-Upcoming
  # run on tag creation
  create:
    tags:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          # Clone the repo to a subdirectory, so we can initialize the Zephyr
          # workspace in the parent directory.
          path: revolutefirmware

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: 3.11
  
      - name: Setup Zephyr project
        uses: zephyrproject-rtos/action-zephyr-setup@v1
        with:
          app-path: revolutefirmware
          toolchains: arm-zephyr-eabi
          sdk-version: 0.17.0

      - name: Build firmware
        run: |
            ls
            pwd
            ls
            west build \
              --board bbc_microbit_v2/nrf52833 \
              --pristine=always revolutefirmware \
              -- \" 

      - name: 📦 Save BIN
        uses: actions/upload-artifact@v4
        with:
          name: ZephyrBin
          path: revolutefirmware/build/zephyr/zephyr.bin

      - name: 📦 Save HEX
        uses: actions/upload-artifact@v4
        with:
          name: Zephyrhex
          path: revolutefirmware/build/zephyr/zephyr.hex

      - name: 📦 Save merged hex
        uses: actions/upload-artifact@v4
        with:
          name: MergedHex
          path: revolutefirmware/build/zephyr/merged.hex


      - name: 📦 Save Update File
        uses: actions/upload-artifact@v4
        with:
          name: UpdateBin
          path: revolutefirmware/build/zephyr/app_update.bin

     



          
